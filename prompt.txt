Two tasks still pending from the previous prompt (likely truncated). Please execute both.

# 1. Update docs/DECISIONS_LOG.md

Append four architectural decisions:

- D06 — Projeto Supabase criado: ref `lacgthfskkcrhmfahjzj`, regiao Sao Paulo (sa-east-1), plano Free, projeto dedicado e independente.
- D07 — Conta Render em plano Free durante o MVP: aceita cold start ~30s ate cliente pagante validar o produto.
- D08 — Branch principal `main`: modernizado de `master` para `main`. Atualizar tambem P05 no mesmo arquivo: trocar `git push origin master:main` por `git push origin main`.
- D09 — Claude Code como agente executor de Python e docs: versao 2.1.148, CLI no PowerShell, pasta `C:\Users\eduar\code\safra-radar`. Nunca executa git (ver P05).

Append three lessons learned from Phase 0:

- L01 — Agentes executores saem de escopo sem guardrails explicitos. Na Fase 0 o Claude Code inventou CLAUDE.md, pre-criou subpastas dentro de api/, e pulou o rename README.md->OVERVIEW.md na primeira passada. Licao: prompts enxutos no "como", explicitos no "o que nao fazer" e renames.
- L02 — Validacao humana e o checkpoint critico da cadeia. Sem revisao manual contra o prompt original, desvios silenciosos passam.
- L03 — Git e responsabilidade humana, mesmo quando o agente oferece executar.

Append one new work pattern:

- P07 — Encerramento de secao exige atualizacao de doc + retrospectiva. Toda secao encerra com: (a) DECISIONS_LOG atualizado com decisoes e licoes novas, (b) DEBT.md atualizado se houver debito tecnico novo, (c) retrospectiva pessoal em `retrospectives/YYYY-MM-DD.txt` (nao versionada, narrativa, escrita sem consultar retrospectivas anteriores).

# 2. Create docs/DEBT.md

Living list of technical debts (not architectural decisions). Short header explaining purpose. Initial bullets:

- Senha do Postgres Supabase nao configurada/registrada — resolver antes da Fase 2.
- `.gitattributes` para normalizar LF/CRLF — criar quando warnings incomodarem ou primeiro colaborador externo entrar.
- Ultima linha de `docs/OVERVIEW.md` provavelmente ainda referencia `README.md` — verificar e corrigir.
- Plano Render Free com cold start ~30s — upgrade apos primeiro cliente pagante (alinhado com D07).

After both tasks, print summary of files modified/created. Do not commit, do not push.
